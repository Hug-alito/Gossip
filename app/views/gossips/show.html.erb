<% if flash[:success].present? %>
  <div class="alert alert-success custom_alert" role="alert">
    <%= flash[:success] %>
  </div>
<% end %>

<div class="container" style="height:80vh">
  <% @gossips.each do |gossip| %>
    <% if gossip.id == @gossip.id %>
      <h3><%= gossip.title %></h3>
      <p><em><%= gossip.content %></em></p>
      <p>Créé par : <%= gossip.user.first_name %></p>
      <p>Dans la ville de : <%= gossip.user.city.name %></p>
      <a href="<%= root_path %>" class="btn btn-primary m-3 rounded-pill">👈 Revenir au menu</a>
      <a href="<%= user_path(gossip.user) %>" class="btn-auteur btn btn-primary m-3 rounded-pill">Profil du gossiper <%= gossip.user.first_name %></a>
      <a href="<%= edit_gossip_path(gossip.id) %>" class="btn-auteur btn btn-primary m-3 rounded-pill">Éditer le potin</a>
      <%= button_to gossip_path(gossip.id),
                    method: :delete,
                    data: { confirm: "Êtes-vous sûr de vouloir supprimer ce potin ?" },
                    class: "btn-auteur btn btn-primary m-3 rounded-pill",
                    form_class: "d-inline" do %>
        Supprimer le potin
      <% end %>
      <hr>
    <% end %>
  <% end %>
</div>
<div class="container">
  <h1>Nouveau Commentaire</h1>

<%= form_tag(gossip_comments_path(@gossip.id), method: 'post') do %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= label_tag :content, "Le contenu de votre commentaire:" %><br>
  <%= text_area_tag :content, nil, size: '60' %>
    <%= submit_tag "Envoyer", data: { turbo: false } %>
  <% end %>
</div>

<div class="container" style="height:80vh">
<% @gossip.comments.each do |comment| %>
  <p><strong><%= comment.user.first_name %></strong>: <%= comment.content %></p>
  <%= link_to "Modifier le commentaire", edit_gossip_comment_path(@gossip.id, comment.id) %>

  
        <%= button_to gossip_comment_path(@gossip.id, comment.id),
                    method: :delete,
                    data: { confirm: "Êtes-vous sûr de vouloir supprimer ce com ?" },
                    class: "btn-auteur btn btn-primary m-3 rounded-pill",
                    form_class: "d-inline" do %>
        Supprimer le com
      <% end %>
<% end %>
</div>